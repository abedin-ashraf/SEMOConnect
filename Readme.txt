WebRTC: It is a UDP based protocol used for live video call
Benefit: 
1. Browser Support
2. Udp can fallback to TCP
3. Peer to Peer service (We don't need server in between)

FrontEnd -> React and WebRTC API
Backend -> Real Time Server (Signaling Server) (Node/Go)
DB -> 
